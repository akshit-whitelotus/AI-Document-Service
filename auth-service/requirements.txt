fastapi==0.136.3
uvicorn[standard]==0.51.0
pydantic==2.13.4
pydantic-settings==2.7.0
email-validator==2.2.0

# Database (async app + sync Alembic migrations)
sqlalchemy==2.0.36
alembic==1.14.0
asyncpg==0.30.0          # async Postgres driver used by the running app
psycopg[binary]==3.2.3   # sync Postgres driver used only by Alembic

# Auth
python-jose[cryptography]==3.3.0   # RS256 JWT encode/decode + JWKS
argon2-cffi==23.1.0                # password hashing
slowapi==0.1.9                     # rate limiting on /login, /register

python-multipart==0.0.20

pytest==8.3.4
pytest-asyncio==0.25.0
httpx==0.28.1
asgi-lifespan==2.1.0
aiosqlite==0.20.0        # test-only: in-memory DB for the test suite
